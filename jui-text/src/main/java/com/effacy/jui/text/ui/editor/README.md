# Editor

## Design

### JS support

Various JavaScript support functions are declared in `EditorSupport` and implemented in `jui_text_editor.js` (making use of `JsType`) which is loaded by the initialiser (`Initialiser`).